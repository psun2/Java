<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:util="http://www.springframework.org/schema/util"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.3.xsd">

	<!-- 객체 생성 -->
	<bean id="target" class="spring.aop.entity.NewLecExam" p:kor="10" p:eng="50" p:math="1" p:com="15" />

	<!-- around 방식의 proxy 생성 -->
	<bean id="logAroundAdvice" class="spring.aop.advice.LogAroundAdvice" />

	<!-- before 방식의 proxy 생성 -->
	<bean id="logBeforeAdvice" class="spring.aop.advice.LogBeforeAdvice" />

	<!-- after returning 방식의 proxy 생성 -->
	<bean id="logAfterReturningAdvice" class="spring.aop.advice.LogAfterReturningAdvice" />

	<!-- throwing 방식의 proxy 생성 -->
	<bean id="logThrowingAdvice" class="spring.aop.advice.LogThrowingAdvice" />

	<!-- Point Cut -->
	<bean id="classicPointCut" class="org.springframework.aop.support.NameMatchMethodPointcut">
		<property name="mappedName" value="total" />
	</bean>
	
	<!-- 포인트 컷 과 어드바이스 연결 -->
	<bean id="classicBeforeAdvisor" class="org.springframework.aop.support.DefaultPointcutAdvisor">
		<!-- advice 와 pointcut 는 정해져 있는 것이므로 변경 불가 -->
		<property name="advice" ref="logBeforeAdvice" />
		<property name="pointcut" ref="classicPointCut" />	
	</bean>

	<bean id="classicAroundAdvisor" class="org.springframework.aop.support.DefaultPointcutAdvisor">
		<!-- advice 와 pointcut 는 정해져 있는 것이므로 변경 불가 -->
		<property name="advice" ref="logAroundAdvice" />
		<property name="pointcut" ref="classicPointCut" />	
	</bean>

	<!-- proxy 객체 생성 -->
	<bean id="proxy"
		class="org.springframework.aop.framework.ProxyFactoryBean">
		<!-- target : setter
		원래이름은 setTarget 이나, 
		target이란 메소드를 직접 호출 할땐느
		약간의 규칙에 따라라 set은 빼주고 앞에문자는 소문자로 변경합니다.
		 -->

		<!-- loader 삽입 -->
		<property name="target" ref="target" />

		<!-- 핸들러 삽입 -->
		<property name="interceptorNames">
			<list>
				<value>logAroundAdvice</value>
				<value>classicBeforeAdvisor</value>
				<value>logAfterReturningAdvice</value>
				<value>logThrowingAdvice</value>
			</list>
		</property>
	</bean>

</beans>
