-- 테이블 생성
CREATE TABLE MEMBER (
userID VARCHAR2(20),
userPassword VARCHAR2(61)
);

-- 생성 확인
SELECT * FROM all_all_tables;
SELECT * FROM all_all_TABLES WHERE TABLE_NAME = 'MEMBER';

SELECT * FROM TAB;

-- 스키마 확인
DESC MEMBER;